# GovWatch

Read PLAN.md first. It holds scope, tech choices, and the published gate.
STATUS.md is the loop log; append one line per increment.

Rules:
- Facts only. Every number on a page carries a source URL and as-of date.
- No frameworks, no bundler. Stdlib Python for fetching. Static HTML/CSS/JS.
- Change behaviour, change PLAN.md or README, same commit.
